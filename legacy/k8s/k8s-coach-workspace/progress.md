# 學習進度卡 (斷點續傳)
<!-- 每次 session 開始前更新這裡,讓 Claude 快速掌握你的狀況,避免重複解釋背景 -->

## 基本資訊

| 欄位 | 內容 |
|------|------|
| 目前 phase | **P2a 網路深水區**(P1 已畢業 2026-06-25,P0 2026-06-22)|
| 目前主題 | **P2a chunk 2 Ingress 全畢業 ✅(s14 07-09 收尾 + F/G 補完)**。keystone 全打穿:①物件=規則(apply Ingress 無 controller→ADDRESS 空)②Controller=引擎 ③L7 分流靠 Host+path 純字串比對。s14 補完 D 段收尾實證四件:分流 body(/api→api、/web→web)、no-Host→404(curl 自動填 URL 主機名當 Host、≠shop.com→無規則接)、`/apiv2`→200 走 web(Prefix 以斜線切段、`/apiv2`≠`/api` 段 → 掉 `/` catch-all)、`/api/v2`→200 走 api(段語義反向確認)。選做 scale=0→503 未做(下次開場 30 秒補)。chunk 1 先前已全畢業。**下一步=chunk 3 NetworkPolicy**(kindnet 不支援、要另開 p2a Calico 叢集,大概率下堂開場先開叢集)→ 之後 chunk 4 CNI+封包全鏈路 |
| 上次 session 日期 | 2026-07-09 |
| session 累計次數 (session_count) | 14 |
| 上次 Weekly Review (last_weekly_review) | 10 |

## 診斷結果 (New Student Warm-Up)

程度: 中 (有地圖形狀,缺演員名字)。pacing = P0 剛好,不加速。

- 強: 已有 reconcile 直覺 (原話「比對現有環境進行更新」);知道 rolling update 替換 Pod。
- 洞 1: 用「下指令」框架 = 還停在 imperative 思維 (k8s 是 declarative)。
- 洞 2: 各步驟「誰做的」全省略 (API Server / etcd / controller / scheduler / kubelet 都沒提到)。
- 洞 3: 漏掉 Pod 怎麼被排到某台 node (scheduler 指派 + kubelet 啟動)。

## 未完成 lab

- [x] D 段 kind lab:apply hello-nginx → 用 `kubectl get events --sort-by` 看五棒 → delete pod 看 reconcile 自動補(新尾碼/新 IP)→ scale=0 證明改 DESIRED 才停。全做完。
- [x] **架構定案(2026-06-22):全部一個 repo = 本 skill repo `k8s-mastery-lab-skill`(= `~/jason/k8s-coach`,remote 已設,push 正常)**。學習產出放 `portfolio/`(notes/ 筆記+踩坑、manifests/ 練習)。舊的獨立 `~/jason/k8s-portfolio` 已刪除合併。見 memory [[k8s-portfolio-value-gate]]。P0 apply→Running 圖太基礎,學員畫私人筆記本即可,不進 repo。

## 下一步

> **課程戰略規劃(2026-07-09 Fable 制定,s15 起生效)**:見 `k8s-coach-workspace/curriculum-plan.md`。重點:capstone 主線已拍板(shop platform 一路長到 P6)/ 面試窗口 2027 農曆新年、策略=紮實加深不趕 / CKA 升正式里程碑(P5 後 sprint)/ 新增 migration 模組與選修池 / 新機制 **M1 why-first、M2 三分類牌、M3 一句話精準版、M4 story mining** 下堂(s15)就上,M2 首發拿 conntrack 重抽當開刀題。教練每過一個 phase gate 回寫該檔 §5 時間軸。
> 下次繼續(一句話):**P2a chunk 3 NetworkPolicy 開講**。kindnet 不支援 NetworkPolicy,要另開一個帶 Calico 的 p2a 叢集(下堂開場先處理開叢集,再進 C 段概念)。教材 `references/phase-2a-networking.md`(NetworkPolicy 段)。**開場先補 chunk 2 選做 30 秒**:`kubectl scale deploy shop-api --replicas=0` → curl `/api` 應回 503(Endpoints 空=後端層 vs 404=規則層,兩層對照),做完 `--replicas=1` 復原。
> **s14 (2026-07-09) 教學紀錄**:P2a chunk 2 Ingress **收尾 + F/G 補完 → chunk 2 全畢業 ✅**。在家這台(context `kind-k8s-coach-p0`,s12 環境;測法用叢集內 netshoot pod curl `ingress-nginx-controller.ingress-nginx.svc.cluster.local`,非 port-forward)。① **A 段抽考**:A-1 Ingress Controller(英文)七成〔L7/watch/執行分流有,漏 apply 無 controller 的後果〕→ 靠 `get ingress` ADDRESS 空自己補回;A-2 thundering herd **作廢**(課表把英文詞印出來=學員照抄,測不出 recall,保留 07-10 真測);A-3 ImagePullBackOff 2/3〔image/tag、401 有,漏 node 出網 i/o timeout,G 段沒收、下次補〕。② **判讀亮點**:controller Running 但 ADDRESS 空 → 教「ADDRESS 抄自 controller Service 的 EXTERNAL-IP,kind 無 LB provisioner→`<pending>`→抄不到」,ADDRESS 空有兩病因(沒引擎 vs 有引擎沒地址),要看 `get pods -n ingress-nginx` 不能只看 ADDRESS。③ **D 段收尾**實證四件(見目前主題),`/apiv2`→200 走 web 是**意外高值**(比原計畫 404 更強:證 Prefix 真按段判、且撞出 `/` catch-all 生產套路);`/api/v2`→200 走 api 反向確認段語義,學員押注押中且講對「多開一層 vs 改長一段」。④ **F 教菜鳥**:第一輪獨白**偏薄**(只講規則 vs controller,漏「比對用 Host+path 兩個」+ 沒解釋 404/200),菜鳥追問「curl 沒 -H 帶的 Host 是什麼字」**連卡→縮成填空**才補出「長 DNS≠shop.com→無規則→404」;`/apiv2`→catch-all 那刀自己串對沒鷹架=最佳時刻。⑤ **G 面試(英文)**:404 排障方向對(先查規則層 ✅),但「規則全綠後兇手在哪」**連卡兩次**(忘記兩條教訓、誤猜兇手=nginx-ingress 本身)→ 教練直接給(真兇=壞掉的 port-forward / 測試路徑那層)。**今日 pattern:結果預測準、why 講不出(隱性會沒逼成顯性會)**,兩筆入 registry(no-Host 404 why / port-forward 排障)皆 +2 天格待重測。**Scorecard P2a-P3:原理🟡 機制✅ 自己的話🟡 MTTR🟡**。**語言:收尾/drill 堂,英文推重(G 段英文作答 + Polish),學員英文可交付但短。**
> **環境(s13 07-07 bastion,最新):context `kind`,kind p0 三節點在、ingress-nginx controller Running(worker 貼 `ingress-ready=true`)、Ingress `shop-ingress` 已 apply,port-forward 用 8081(舊 8080 壞掉別用)。lab 檔 `portfolio/manifests/ingress-demo/`。**
> **環境(s12 07-03 那台,歷史):kind `k8s-coach-p0`,ingress-nginx 1/1 Running(cloud 版 EXTERNAL-IP `<pending>` 是 kind 正常)。測法用叢集內 netshoot pod `curl -H "Host: shop.com" http://ingress-nginx-controller.ingress-nginx.svc.cluster.local/[api]`。lab 檔 `portfolio/manifests/ingress-lab/`。兩份 lab 檔都在 repo。metrics-server 仍缺(P3 HPA 前補)。**
> **session 12 (2026-07-03) 教學紀錄**:P2a chunk 2 Ingress D段 lab 全做完(A複習+D動手+keystone誘答)。① A段抽考兩題全 PASS:liveness/readiness 最壞情境(判斷句「重啟能不能解決」+ 正回饋迴圈 + 羊群效應概念都自己講出,英文詞 thundering herd 忘了補回,推 07-10);API Server 英文卡(內容窗口+唯一碰etcd 對,但用中文答沒用英文;學員自己追問「為什麼這樣設計」→ 引導出攻擊面/介面抽象化/集中把關三層 = senior 面試角度,推 07-10 下次逼英文)。② D段=今天主菜,刻意「先 apply Ingress 讓它躺著沒 controller→再裝 controller 看它活」的反序教法,親眼證三件事(見上目前主題)。③ **意外高值副線**:學員手打 YAML 連環 typo(縮排塌/apiversion 小寫/numer/prefix 小寫/name 少縮一層/port vs targetPort 打錯數字)→ 教 `--dry-run=client`(本地只驗結構,unknown field/enum 放行)vs `--dry-run=server`(送 API Server 走 strict decoding、噴 unknown-field 藏寶圖),學員親眼看同檔 client 綠燈 server 打槍。回扣 registry 2026-06-18 unknown-field 那條。**新坑入帳(見 mistake-registry 2026-07-03)**。④ **超棒 naive-but-deep 提問**:「我們又沒有 shop.com 這 domain,憑什麼能測?」→ 拆兩層(連得到=DNS/L4 vs 轉哪個後端=L7 Host header 字串比對);學員自答「改 /etc/hosts 就不出去問外部 DNS」=連得到層對,教練補 curl -H 偽造 Host 字串 + 對照組 404 證 nginx 純字串比對。⑤ 誘答「kube-proxy 關掉沒差」PASS,學員指出 ClusterIP 層斷;教練補精度(全叢集 ClusterIP+CoreDNS 全掛、外部 NodePort 也 kube-proxy 開的)+ 點出陷阱陰險處(nginx→後端那跳確實繞過 kube-proxy=半真,但要先進得了 nginx 靠 kube-proxy)。**未做 F/G(學員選 B 收工沉澱)**。**語言:延續中文為主、術語錨英文,Say-it-in-English 輕推**。
> **session 11 (2026-07-01) 教學紀錄**:走 Service type 階梯把 Ingress 用第一性原理長出來,全程 Feynman 引導他自己推。① A 段抽考:conntrack(英文,概念+映射方向對,但「table full 新 vs 舊」精度掉了→復述問題沒定案,教練直接補「舊連線照常/新連線被 drop」,推 07-04);busybox DNS 排障坑一開始**把 conntrack 誤拉進 NXDOMAIN 題=層級混淆**(DNS 解析層 vs 連線/NAT 層),拆解後守住「先用 FQDN 測→測得到就別重啟 CoreDNS」的操作判斷(推 07-08)。② 中途一度「不太懂」→ 退回更小步、改用 ASCII 圖(NodePort 三痛點視覺化)重帶,恢復。③ **階梯全自己推對**:node IP 是外部唯一摸得到的 → NodePort 複用 kube-proxy DNAT(自答)→ NodePort 痛點(醜網址/單點/port 受限)→ NodePort 真實場景(他問「只有測試?」→補地端 F5/HAProxy、雲上疊在 NodePort 上)→ type:LoadBalancer(他把 provisioner 答成「controller」**其實半對**=cloud-controller reconcile loop 去雲上生 LB,複利回扣 P0)→ 痛點 A(N 服務=N 台 LB×成本)+ 痛點 B(**他自答 L4/NLB 看不到 URL path**=最關鍵一刀)→ Ingress 兩個第一性理由(省 LB + L4→L7)。④ **keystone 誘答 PASS**:「apply Ingress 物件流量就自動分流嗎?」→ 他秒答 No,並從 kube-proxy「寫規則 vs 搬封包」遷移推出執行者=**Ingress Controller (nginx/traefik Pod)**,自己講出 nginx-ingress。定型三合一對照表(kube-proxy/type:LB/Ingress 都是「規則=資料 vs 執行=引擎」同模式)。新術語卡:Ingress Controller(07-04 抽)。**未做 D 段 lab(學員選 b 收工)**。phase-2a 教材檔仍未建,從第一性原理教。**English Ramp:user 全域已改 70% EN/30% 中(2026-06-28 CLAUDE.md),但學員今天明說「中文佔比多一些」→ 中文為主、術語錨點英文,Say-it-in-English 輕推不硬逼**。EKS P2a 起可進場(terraform 指令由 user 親手跑,命名 `billing-dev-eks-*`)。**⚠️ Weekly Review 已到期(9-0≥7),chunk 1 收完(或下次 A 段)該插一次**。
> ⚠️ **安全鐵律(2026-06-25 現場抓到)**:user 的 prod kubeconfig `KUBECONFIG=/home/ec2-user/.kube/eks/config.yaml` 的 context `eks` 指向公司 PROD `billing-devops-prod-platform`。**本地 kind 的 context 實際叫 `kind-k8s-coach-p0`**(lab-cluster.sh up 自動設成這個,非舊筆記寫的 `kind`)。每次動手 lab 前先 `kubectl config current-context` 確認是 `kind-k8s-coach-p0` 才 apply。**2026-06-28 開 lab 時實測 current-context 為空(此 shell 未載入 prod kubeconfig),起完 kind 後自動指向 `kind-k8s-coach-p0`,已確認安全**。**2026-07-07 補**:學員也在公司 bastion `billing-eks-bastion` 上做 lab,那台本地 kind 的 context 叫 **`kind`**(不是 `kind-k8s-coach-p0`),已由學員確認 + 實體佐證(`get pods` 顯示 node `k8s-coach-p0-worker`、Pod IP `10.244.x.x` = kindnet 網段)= 本地 kind。**判準:context 是 `kind` 或 `kind-k8s-coach-p0` 都安全(本地 kind);只有 `eks` 才是公司 PROD `billing-devops-prod-platform`。別再因 context 名叫 `kind` 就誤觸警報卡住學員**。
> 環境(2026-06-28 重建):舊叢集已不在,用 `bash scripts/lab-cluster.sh up p0` 重新建好,**3 節點全 Ready**(control-plane + worker + worker2),CoreDNS 2 副本 Running。**⚠️ metrics-server 沒了**(隨舊叢集消失),P3 HPA 前要重裝(kind 需 `--kubelet-insecure-tls` 旗標)。無殘留 Pod(乾淨)。lab 檔放 `portfolio/manifests/`。學員偏好自己敲指令,YAML 預設給規格。學員用英文作答時附 `💬 English Polish`;教完流程主動附英文 mind map 供手抄默畫。
> commit 規則:user 全域禁止任何 trailer/Co-Authored-By,commit message 一行。
> 待補精準度:術語要用「DESIRED vs CURRENT」「reconcile loop 收斂」而非「監控數量」。**新補洞**:只有 API Server 直接讀寫 etcd,其他元件(controller/scheduler/kubelet)都透過 API Server 的 watch/update,不直接碰 etcd(P0 學員口誤「kubelet 寫回 etcd」)。etcd Raft 深入 park 到 P5/foundations。
> English Ramp:P1 仍維持「只做術語卡」(P0-P1 同一檔位),P2a 起才加英文短句。

## 補充筆記

- 學員背景: DevOps 工程師,hands-on 有 (kubectl apply / 看 logs),底層理論弱;coding 初學。
- 教法備忘: 多用生活 analogy、用學員原話回扣、一次一個 chunk、語言要白。
- chunk map (P0): [1] declarative+reconcile → [2] API Server+etcd → [3] controller 怎麼 reconcile → [4] scheduler 指派 node → [5] kubelet 啟動容器。全部 ✅。
- chunk map (P1): [1] container=namespace+cgroup 圈的行程(vs VM 共用 kernel)✅ → [2] Pod 為何存在(共享 network ns,pause container,sidecar,同生共死才放同 Pod)✅ → [3] probe ✅✅(D/E 段動手做完:busybox+exec probe 用 /tmp/ready /tmp/alive 信物,rm readiness→endpoint 消失但 RESTARTS 0、STATUS 仍 Running;rm liveness→RESTARTS+1 後 command 重 touch 自癒。學員兩次預測全中,還自己從「rm:No such file」反推出「尚未 restart」。偵測延遲=failureThreshold×periodSeconds=調校旋鈕,回扣 liveness 查 DB 雪崩。lab 檔在 portfolio/manifests/probe-demo.yaml)→ [4] Deployment/rollout ✅✅(見下 session 4)→ [5] resource/QoS/OOM ✅✅(見下 session 5)。
- chunk map (P2a): [1] Service/kube-proxy/CoreDNS → 謎題A Service→Endpoints ✅ / 謎題B ClusterIP→DNAT 去中心化 ✅(有鷹架過,欠 D段 iptables 實體冷確認)/ 謎題C conntrack ✅(NAT 雙向改寫要記憶/table full 新連線 drop 舊的 OK/node-kernel 層 kubectl 看不到/查法 conntrack -L、/proc/.../nf_conntrack_count vs _max、dmesg table full)/ CoreDNS ✅(叢集 DNS server=kube-system 幾個 Pod/resolv.conf 由 kubelet 注入指向 CoreDNS ClusterIP/跨 ns 要 FQDN service.ns.svc.cluster.local/排障階梯:get svc+endpoints→CoreDNS pod 活著嗎→進出問題 Pod nslookup) / D段lab ✅(2026-06-28 親手 apply backend Deploy+Svc、get endpoints 對 Pod IP、docker exec node iptables-save 追 KUBE-SERVICES→SVC機率LB→SEP DNAT 完整鏈、worker/worker2 一致證去中心化、nslookup 撞 busybox DNS 坑當場 debug)。**chunk 1 全畢業 ✅**。[2] Ingress **全畢業 ✅**(C 段概念 s11 + D 段 lab s12/s13/s14 全實證 + F/G s14 補完;分流 body / no-Host→404 純字串比對 / Prefix 段語義 + `/` catch-all 都親手驗過)。後續 [3] NetworkPolicy(下堂開講,要 Calico 叢集)[4] CNI+封包全鏈路(教材 `references/phase-2a-networking.md` C-1~C-5 全建好)。
- session 13 (2026-07-07): **P2a chunk 2 Ingress D 段 lab**(公司 bastion,context `kind`)。開場 pull 進大量新教材(`references/phase-2a~6` + `story-bank.md` 全建好,phase-2a-networking.md 有完整 C-2 Ingress lab 腳本 Step0~6)。**A 段抽考**:① kube-proxy(英文題他用中文答=輕推不硬逼)— 守住「**不搬封包**」誘答 ✅,但「用來**改寫** ip」精度掉 → 磨回「kube-proxy 只**寫規則**、**kernel 執行 DNAT**」立法者 vs 執法者(他 s8 講對過、今天略退)② DNS 排障第一刀「忘記了」→ 給梯子(短名字靠 resolv.conf `search` 補全、busybox 補全有 bug)引導出「**先用 FQDN 二分伺服器壞 vs 補全壞**」,他自己定位到「問題在 resolv/search 端」✅(需鷹架)。**D 段 lab**:Step1 佈 shop-api/shop-web(`hashicorp/http-echo`)+ **帶讀 backends.yaml 逐塊**(他主動要求「一行行帶我看檔案」,不當黑箱)— Deployment `replicas` vs `matchLabels`(回扣他 matchLabels 打錯坑)、Service **`port:80` vs `targetPort:5678`**(埋 502 伏筆);`get endpoints` 預測「2 個 **Pod IP** 非 ClusterIP」全對。Step2 **親手寫 ingress.yaml(他選「給我看照打」)犯兩錯**:`backend.service: shop-api`(該是 object)+ `pathType: prefix`(該大寫 `Prefix`)→ 用 `--dry-run=server` 讀 validation error 自己修對。**大發現(學員自己跑出來)**:`--dry-run=client` 回 `created` **騙人**、`--dry-run=server` 才擋下 = client 給假安心(進 registry)。Step2 payoff:apply 後 `Events <none>` + `ADDRESS` 空 = 「規則沒引擎」實體證據,三題預測全對(「只設規則、沒 nginx controller pod 轉發」)。Step3 裝 ingress-nginx → controller **Pending** → `describe` 讀 `FailedScheduling: 0/3 nodes ... didn't match node affinity/selector` → 找 `Node-Selectors: ingress-ready=true` 沒貼 → `label node ... ingress-ready=true` → **自己排上去 Running**(P0 scheduler filter + reconcile 複利,他親眼看「只加標籤、沒 delete,Pending 自癒」)。Step4 `grep nginx.conf shop`=16 = 規則被 render 成 nginx.conf 實體證據(desired=Ingress/actual=nginx.conf 的 reconcile)。**Step5 curl 撞 404 → MTTR 排障(今天最有價值段)**:照 404 階梯查 → `get ingress` ADDRESS=`localhost`(認領 ✅)/ `get ingressclass` nginx 存在 ✅ / nginx.conf 有 `server_name shop.com`(429 行 ✅)= **規則層全綠** → 換乾淨 port-forward **8081** → `/api /api/ /web /web/` **全 200**。**真兇=被 `Ctrl-C` 打斷的舊 port-forward(8080)半死回假 404,不是 Ingress 規則**。教訓兩條(已進 registry):① 規則全綠時兇手在「**你測試所經過的那層**」,別去改沒壞的規則 ② `kubectl port-forward` 是會抖的除錯夾具、不可信就換乾淨再下結論。(coach 中途猜「trailing slash」被資料證偽 → 順勢示範「先驗證再下結論,包括不信教練的猜測」)。**未做完(下次收尾)**:分流 body 確認(/api→api /web→web)+ 2 負向 case(不帶 Host→404、/apiv2→404 Prefix 段語義)+ 選做 Step6 scale=0→503。**語言中文為主**(新難主題堂,英文降回術語卡層級=SKILL 新錯峰規則)。
- session 10 (2026-06-29): **Weekly Review #1**(觸發 9-0≥7;取代正常 session)。三主題盲講全 PASS。**① P2a 封包全鏈(冷測)**:無鷹架一口氣講完 DNS→DNAT→回程,06-27 崩的「封包先去 ClusterIP」沒再犯,**謎題B Gate 二度封印**。補兩洞:(a) resolv.conf 裡的 IP=**CoreDNS 的 ClusterIP**(nameserver),不是 backend 的;兩步「resolv.conf 給去哪問、CoreDNS 給 backend 是誰」他壓成一步,已拆;(b) 回程「做記錄的東西」名字想不起來→補 **conntrack**(概念本來就懂)。**② P1 resource/QoS/OOM**:137/OOMKilled/CPU throttle 表面全對,但他**自己誠實標出「壓縮是啥意思不懂」**=真洞→用「水龍頭流速 vs 水桶存量」兩場景引導他自己推出第一性原理:memory 已佔的位元組無法縮小、kernel 唯一回收手段=殺掉整個 process→OOMKilled。**洞打穿**。QoS 驅逐序 BestEffort→Burstable→Guaranteed + 理由(沒繳訂金/硬約定)全自己講對。給面試英文一句話版。**③ P0 apply→Running 控制流**:盲講一口氣**漏掉 scheduler**(正是他 P0 原始洞3),一被問「哪台 kubelet、誰決定 node」秒抓回;etcd「唯一 API Server 直接碰、其他 watch/update」釘對(P0 口誤正解)。**Scorecard P2a-P3**:底層原理✅/內部機制✅/自己的話✅/MTTR 今日未演練(carry 前測✅)。**最佳時刻**=主題1全鏈無鷹架冷測。**最該改進**=盲講控制流容易漏中間棒次→建議用「五棒」當固定骨架默數到5。Portfolio 稽核:notes(P1×5+P2a×1)+manifests(P0/P1/P2a)齊,P0 概念性故只留 lab manifest 正常,showcase 主秀(observability/gitops/terraform-eks)待 P3+ 長出。**下一步=chunk 2 Ingress**。
- session 9 (2026-06-28): **接 session-7 斷點收 chunk 1 概念三關 + 起 kind**。⚠️注意:session 啟動讀到的是 session-7 舊快照(stale),教學中途檔案才同步到 session-8 後狀態 → 我已用電話比喻重收謎題B(學員上次冷測崩在此)。**謎題B**:學員把電話比喻 mapping 回 k8s,分機/轉接表/總機初次貼錯(分機=PodIP 他答 endpoint、總機小姐=被刪掉的中央LB 他答 ClusterIP),引導後總結句「pod 指定 ClusterIP→本機 iptable 被 kube-proxy 查 endpoint 替換→**在本身 node 就被改寫**」→ 正面打掉「封包先去 ClusterIP 拿 IP」誤解,Gate 過(有鷹架)。**謎題C conntrack**:用「回程封包來源是 Pod-A IP、client 只認 ClusterIP 會丟掉」逼出 conntrack 存在理由,學員秒答「會丟、陌生 IP」;遷移題 table full 症狀答對(新連線失敗),查法誤答 `kubectl get conntrack` → **抓到超有價值的觀念坑:conntrack/iptables 是 node kernel netfilter、非 k8s 物件、kubectl 永遠看不到**,學員自己推回「node 本機、類似 iptables」釘正。**CoreDNS**:用「app 寫名字 vs iptables 只認 IP」矛盾逼出 DNS,學員秒答 DNS server;遷移題「could not resolve host」正確鎖定 DNS 段(非 DNAT 段)+ 先查 CoreDNS,補排障階梯與跨 ns FQDN 坑。給英文 mind map(放射狀,Service→Endpoints/ClusterIP→kube-proxy/iptables/DNAT/conntrack/CoreDNS)。**起 kind p0 成功**(safety check 先行)。新術語卡:conntrack。**語言**:user 全域改 70%EN 但學員當場要求中文為主,已遵從。**下次=D段 lab 做 iptables 實體冷確認**。
- session 8 (2026-06-27): **壓測新 F 段「Teach the Rookie」**(skill 剛從 sd-coach 移植教菜鳥機制,去角色化 + naive-but-deep 加壓)。題材=謎題B(ClusterIP/kube-proxy/DNAT),正好收上次未過的 Gate。**drill 表現**:① teach-back 獨白很薄(只講到「Pod IP 會變→要 Service→用 kube-proxy」就停),暴露 can-use-but-cant-say。② 菜鳥 3 連問把零件逐一逼出:Q1「ClusterIP 是哪台機器的 IP?」→ 他「不確定」**觸發安全閥**,縮小成「綁 node-1 會怎樣」的思想實驗 → 他自己推回**瓶頸**站起來。Q2「kube-proxy 是不是一直在搬封包?」(**埋誘答陷阱**)→ 他沒接受、改口「只負責紀錄」**反殺陷阱成功**(磨精準=寫 iptables 規則、kernel 才搬封包)。Q3 DNAT 答對✅,但「健康 Pod IP 名單」誤答 iptables(手 vs 名單混)→ 用他 P1 probe lab(rm readiness→從 Endpoints 消失)當線索撈回 Endpoints✅。③ **full teach-back 崩**:「封包先去 ClusterIP 拿 ip…我也不太會」露出反直覺誤解(以為封包會「拜訪」ClusterIP)→ 連兩卡觸發安全閥,不硬逼,給乾淨五步版收腦袋,**Gate 判定未過 🟡**,進 registry 下次冷測。**學員 meta 回饋(已存 memory [[led-along-despite-doubt]])**:超喜歡「埋誘答」這招,因為真實工作他常「覺得怪怪的但沒 100% 把握就被同事牽著走」→ 日後刻意常埋誘答練反脆弱。**F 段壓測結論=有效**:3 問就挖出「自以為懂、其實沒打穿」的 Gate,安全閥兩次正確觸發沒壓垮。
- session 7 (2026-06-26): **P2a chunk 1 開講(Service/kube-proxy)**,C段概念為主,**無 lab/E/F**(學員下班喊停,Gap Mode 存點,不逼 F)。A段抽考:ImagePullBackOff 三類根因訊號(repo-not-exist/connection-refused/401-toomanyrequests)一次答全對推07-03;API Server 英文抽考過(櫃檯比喻+唯一碰etcd,唯一錯 control panel→**plane** 已釘,推07-03);P0 etcd 口誤補正(scheduler/kubelet 透過 API Server 不直接寫 etcd,答對)。**今日主線(全程 Feynman 引導他自己推)**:① Service 為何存在=Pod IP ephemeral 寫死必爆(秒答)② Service 用 label selector 框 Pod(遷移 P1 matchLabels,他自己答 label)③ Endpoints/EndpointSlice=健康 Pod IP 清單,controller 跑 reconcile loop 維護、readiness 當閘門 → **回扣 P1 probe lab「rm readiness→從 Endpoints 消失但 RESTARTS 0」完美解釋**(reconcile 第3次出現=複利伏線)④ ClusterIP 是虛擬 IP、沒綁任何網卡 ⑤ 思想實驗「若綁 node-1 一張網卡」→ 他自己答出**瓶頸點**+單點 ⑥ 揭曉 kube-proxy 每台 node 都跑+寫 iptables 規則,封包出發 node 本地就 **DNAT 改寫目的地** → 去中心化無瓶頸。**卡點**:DNAT/去中心化他說「有點不太懂」,給電話代表號比喻(代表號=ClusterIP/分機=PodIP/話機轉接表=iptables規則/總機小姐=中央LB瓶頸),要他 mapping 回來時喊下班 → **Feynman Gate 卡在這未確認,下次先收這個**。學員 CNI 誤會已拆(CNI 管 Pod 拿真實 IP/Pod 互通,非 ClusterIP)。新術語卡2張:DNAT、kube-proxy。筆記 portfolio/notes/p2a-service-kube-proxy.md。
- session 6 (2026-06-25): **P1 畢業 Gate PASS → P1 全畢業**。On-call triage drill:一次 apply 三隻混合故障 Pod(中性命名,blind),他用 `get pods`(全景:STATUS+RESTARTS)→ `describe`(讀 Reason/Exit Code/Events)逐一拆。三隻全定位正確:payment-api=ImagePullBackOff(tag `nginx:1.99` 不存在,驗證邊界:kubelet 第5棒才發現)/ cache-worker=OOMKilled exit137(撞 cgroup mem limit)/ web-frontend=CrashLoop 但 **exit 0 Completed**(liveness probe 打 8080、nginx 聽 80 → probe 殺健康 app,SIGTERM 優雅退出;他從 exit0 反推「app 健康、被 probe 殺」=最佳時刻)。A 段抽考 probe 坑通過(自己講出「羊群效應/thundering herd」+ reconnection 風暴回壓 DB 正回饋,推 +7 到 07-02)。**改進(連續第三 session 同一條)**:先跳結論/修法,要追問才補深度(治標 vs 治本、exit0 意義)→ 帶進 P2a:講完現象+根因主動接「修法+治標/治本+先驗證什麼」。治標 vs 治本他能分(leak 鋸齒爬頂 vs rightsize 平台水位,A=leak B=可調大,工具 kubectl top/Prometheus → 預告 P4)。scorecard P0-P1 全 ✅。新術語卡:thundering herd。mind map(describe 速查表放射狀)已給他手抄默畫。
- session 5 (2026-06-24): **chunk 5 resource/QoS/OOM 全做完(C+D+E+F+G)**。requests=預訂+排程依據(scheduler 加總≤allocatable,不可超賣)/ limits=cgroup 硬天花板(可超賣)。**核心第一性原理:可壓縮(CPU,撞 limit→throttle 變慢)vs 不可壓縮(memory,撞 limit→OOMKilled exit137=128+9 SIGKILL)**,學員自己推出這不對稱(他坦言半年前面 AWS EC2 support 被問 OOM 答不出沒上,今天破了)。**兩種 OOM**:容器級(撞自己 cgroup limit,與 node 餘量無關=B段謎題)vs node 級(kubelet 按 QoS 驅逐)。QoS:Guaranteed(requests==limits)>Burstable>BestEffort,只設 limit 會被自動補成 Guaranteed。D 段 lab oom-demo.yaml(polinux/stress 要150M/limit100Mi)親手重現 OOMKilled+exit137+Burstable+node 仍有餘量,三題全自己讀對。E 段 chaos:CrashLoopBackOff 是現象不是死因→describe 速查表(OOMKilled137 / Error1 / probe / ImagePull);教 `kubectl logs --previous`(他沒用過)。Round2 senior 判斷:leak 調大 limit 只是治標(鋸齒爬頂 vs 平台水位分辨)他自己講出。F 段 mock 兩段全對。scorecard 全 ✅。改進=主動吐「治本vs治標」別等追問。筆記 portfolio/notes/p1-resource-qos-oom.md。新術語卡 3 張:compressible/incompressible/OOMKilled。
- session 4 (2026-06-23): **chunk 4 Deployment/rollout 全做完(C+D+E+F+G)**。教完:Deployment 管 RS 不管 Pod、翹翹板兩 RS、maxSurge(額外上限)/maxUnavailable(可用下限)公式、零停機正解=maxUnavailable:0+maxSurge:1/25%、死鎖 maxSurge:0+maxUnavailable:0 被 apply 退件、rollback 快=舊 RS 留著 scale up 不重拉 image、revision history/CHANGE-CAUSE。**驗證邊界金句**:schema 錯 API Server 當場擋,外部現實(image 在不在)只有 kubelet 第 5 棒拉才知。D 段 lab rollout-demo replicas=4 maxSurge1 maxUnavailable1 minReadySeconds10:正常 rollout→rollback→壞部署(nginx:9.99)卡死幾何 3 好+2 壞=5、3 舊 Pod 全程沒倒。**現場 free chaos**:image 打成 ngimx → ImagePullBackOff,學員自己讀 Events 抓到根因(進 mistake-registry)。F 段 mock(零停機策略+保護+rollback):骨架全對,A1 自己抓出「額外8→總數12」算錯;senior 補刀=別等被追問才講機制、選經濟配置別報 maxSurge:8。scorecard 全 ✅。改進=主動吐機制+挑經濟值。筆記 portfolio/notes/p1-deployment-rollout.md。
- P1 已澄清:Linux namespace(kernel 隔離視野)≠ k8s namespace(邏輯分組,**不做隔離**,擋網路要 NetworkPolicy)。學員自己問出這個撞名點,理解力好。
- session 1 scorecard (P0-P1): 底層原理 ✅ / 內部機制 ✅ / 自己的話 ✅。改進=用詞精準度。
- symptom→棒次地圖已教: Pending=scheduler / ContainerCreating=kubelet 網路volume / ImagePullBackOff=runtime拉image / CrashLoopBackOff=容器或probe。
- session 2 (2026-06-18) D段親手做完: kind 拆解(node=container)、kubeconfig/context 安全(他原本怕打到公司 prod EKS,已教 current-context 肌肉記憶)、Deployment→ReplicaSet→Pod 三層(從 pod 名 hash 看族譜)、reconcile 自癒、scale=0。
- session 2 scorecard (P0-P1): 底層原理 ✅(自己用恆溫器比喻 declarative)/ 內部機制 ✅ / 自己的話 ✅。改進=用詞精準度(同上)。
- session 3 (2026-06-22): P0 畢業 gate 通過(五棒+演員+scheduler/kubelet 分清);兩次 feedback 砍低價值產物(portfolio + 術語卡),skill 已加雙價值門檻。scorecard (P0-P1):底層原理 ✅ / 內部機制 ✅ / 自己的話 ✅。改進=etcd 只有 API Server 直接碰(口誤待修)。
- ~~術語卡 kubeconfig / context / kind~~ **2026-06-22 移除**:學員指出定義型瑣碎詞/工具名面試不考,違反新價值門檻(見 memory [[k8s-portfolio-value-gate]] 同源原則 + SKILL.md 術語卡)。current-context 改記為 ops 安全習慣,不當卡。
