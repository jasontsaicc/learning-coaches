# Language

Optional hook. If absent, the engine default is English only, no ramp. Stamp this file
only when the coach needs a different policy.

## Default Language

<!-- TODO: the default language for this coach. -->

## Ramp Policy

<!-- TODO: when and how the coach introduces a second language (e.g. native-language anchors for key terms first, then shift terminology, then full sentences). State "no ramp" if not applicable. -->
