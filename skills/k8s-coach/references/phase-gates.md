# Phase Gates

The engine enforces the 3-attempt cap and the failure protocol. This hook defines what
"pass" means for each phase: a concrete pass condition tied to observable student
behavior, not a time-on-task target. Add one section per phase.

## Phase 1 Gate

<!-- TODO: a concrete pass condition (what the student must do, unaided, to clear this phase). Add a suggested gate question or task the engine can vary across attempts. State any domain criterion that raises the bar beyond the 60% scorecard (e.g. "must name three failure modes without prompting"). -->

## Phase 2 Gate

<!-- TODO: pass condition for Phase 2. -->

## Phase 3 Gate

<!-- TODO: pass condition for Phase 3. Add more sections to match the curriculum. -->
