# Portfolio

## Workspace Directory

<!-- TODO: the workspace path where progress.md and in-progress work live (e.g. ~/workspaces/k8s/). -->

The progress file `progress.md` lives here. Its schema is engine-owned and defined in
`engine/PROGRESS-SCHEMA.md`; this coach does not redefine it. Declare any optional domain
registries (term, command, etc.) here; they reuse the registry fields from
`PROGRESS-SCHEMA.md` section 7.

## Per-Phase Artifacts

<!-- TODO: for each phase, the artifact the student should have produced by the end of it (a doc, a working config, a saved command output). The engine checks these during Weekly Review's artifact audit. State the format and naming so they can be located. -->
